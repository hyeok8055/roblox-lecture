-- ╔══════════════════════════════════════════════════════════════╗
-- ║  📊 Leaderstats 종합 시스템 (8주차 A+B)                     ║
-- ╠══════════════════════════════════════════════════════════════╣
-- ║  📦 위치: ServerScriptService → LeaderstatsScript           ║
-- ║                                                              ║
-- ║  💡 이 스크립트 하나로 아래 기능 전부 관리!                   ║
-- ║     ✅ 코인(Coins) + 레벨(Level) 점수판                     ║
-- ║     ✅ 죽으면 코인 -5 벌칙                                   ║
-- ║     ✅ 코인 100개 모으면 자동 레벨업!                        ║
-- ║                                                              ║
-- ║  ⚠️ 중요! 이 스크립트가 없으면 코인 파트들이                ║
-- ║     전부 작동하지 않아요!                                     ║
-- ║                                                              ║
-- ║  🔑 핵심 개념:                                               ║
-- ║     • Players 서비스 = 모든 플레이어 관리                    ║
-- ║     • PlayerAdded = 플레이어가 입장할 때 발생하는 이벤트     ║
-- ║     • Instance.new() = 새로운 객체(물건) 만들기              ║
-- ║     • "leaderstats" = 반드시 전부 소문자!                    ║
-- ║     • IntValue = 정수(숫자)를 저장하는 객체                  ║
-- ║     • CharacterAdded = 캐릭터가 생성/리스폰될 때             ║
-- ║     • Humanoid.Died = 캐릭터가 사망했을 때                   ║
-- ║     • GetPropertyChangedSignal = 값이 변할 때 감지           ║
-- ╚══════════════════════════════════════════════════════════════╝

-- ============================================================
-- 🏁 1단계: Players 서비스 가져오기
-- ============================================================
-- 💡 game:GetService("이름") = 로블록스 서비스를 가져오는 함수
-- 💡 Players 서비스 = 게임에 접속한 모든 플레이어를 관리!
local Players = game:GetService("Players")

-- ============================================================
-- 👤 2단계: 플레이어가 게임에 들어오면 실행!
-- ============================================================
-- 💡 PlayerAdded 이벤트 = 누군가 게임에 입장할 때 자동 실행
-- 💡 function(player) = 입장한 그 플레이어 정보가 들어옴
-- 💡 이 안에서 점수판을 만들어 줘야 해요!
Players.PlayerAdded:Connect(function(player)

	-- ════════════════════════════════════════
	-- 📁 3단계: leaderstats 폴더 만들기
	-- ════════════════════════════════════════
	-- 💡 Instance.new("Folder") = 새 폴더 만들기
	-- ⚠️ 이름은 반드시 "leaderstats" (전부 소문자!)
	-- ⚠️ "LeaderStats", "Leaderstats", "LEADERSTATS" → 전부 안 됨!
	-- 💡 이 폴더가 Player 안에 들어가면 → Tab 리더보드에 표시!
	local leaderstats = Instance.new("Folder")
	leaderstats.Name = "leaderstats"   -- ⚠️ 반드시 소문자!
	leaderstats.Parent = player        -- 💡 플레이어 안에 넣기!

	-- ════════════════════════════════════════
	-- 💰 4단계: 코인(Coins) 만들기
	-- ════════════════════════════════════════
	-- 💡 IntValue = 정수(Integer, 숫자)를 저장하는 객체
	-- 💡 .Name = 리더보드에 표시될 열(column) 이름!
	-- 💡 .Value = 저장된 숫자 값 (읽기/쓰기 가능!)
	-- 💡 .Parent = 어디에 넣을지 (leaderstats 안!)
	local coins = Instance.new("IntValue")
	coins.Name = "Coins"       -- 💡 리더보드에 "Coins"로 표시됨
	coins.Value = 0             -- 💡 시작할 때 코인 0개!
	coins.Parent = leaderstats  -- 💡 leaderstats 폴더 안에 넣기!

	-- ════════════════════════════════════════
	-- ⭐ 5단계: 레벨(Level) 만들기
	-- ════════════════════════════════════════
	-- 💡 IntValue를 여러 개 만들면 리더보드에 여러 열이 생겨요!
	-- 💡 Coins 옆에 Level도 같이 표시됩니다!
	local level = Instance.new("IntValue")
	level.Name = "Level"        -- 💡 리더보드에 "Level"로 표시됨
	level.Value = 1             -- 💡 시작 레벨 = 1 (0이 아닌 1!)
	level.Parent = leaderstats  -- 💡 같은 leaderstats 안에!

	-- ════════════════════════════════════════
	-- 🔄 6단계: 자동 레벨업 시스템!
	-- ════════════════════════════════════════
	-- 💡 GetPropertyChangedSignal("Value")
	--    = coins.Value가 바뀔 때마다 이 함수가 실행됨!
	-- 💡 코인이 100개 이상이면 → 레벨 +1, 코인 -100
	-- 💡 예: 코인 120 → 레벨업! → 코인 20, 레벨 2
	coins:GetPropertyChangedSignal("Value"):Connect(function()
		if coins.Value >= 100 then
			level.Value = level.Value + 1        -- 💡 레벨 올리기!
			coins.Value = coins.Value - 100      -- 💡 100코인 사용!
			print("⭐ " .. player.Name .. " 레벨업! → Lv." .. level.Value)
		end
	end)

	-- ════════════════════════════════════════
	-- ☠️ 7단계: 사망 벌칙 시스템!
	-- ════════════════════════════════════════
	-- 💡 CharacterAdded = 캐릭터가 생성(리스폰)될 때마다 실행
	-- 💡 왜 CharacterAdded 안에? → 리스폰할 때마다 새 Humanoid가 생기니까!
	player.CharacterAdded:Connect(function(character)
		-- 💡 WaitForChild = Humanoid가 로딩될 때까지 기다리기
		local humanoid = character:WaitForChild("Humanoid")

		-- 💡 Died 이벤트 = 이 캐릭터가 죽었을 때 실행!
		humanoid.Died:Connect(function()
			-- 💡 math.max(a, b) = a와 b 중 큰 값을 반환
			-- 💡 math.max(코인-5, 0) = 코인이 0 미만이 안 되게!
			coins.Value = math.max(coins.Value - 5, 0)
			print("☠️ " .. player.Name .. " 사망! 코인 -5 → 현재: " .. coins.Value)
		end)
	end)

	print("👤 " .. player.Name .. " 입장! (Coins: " .. coins.Value .. ", Level: " .. level.Value .. ")")
end)

print("═══════════════════════════════════════")
print("📊 Leaderstats 시스템 로드 완료!")
print("   💰 코인 | ⭐ 레벨 | ☠️ 사망벌칙 | 🔄 자동레벨업")
print("═══════════════════════════════════════")
