-- ╔══════════════════════════════════════════════════════════════╗
-- ║  💰 밟으면 코인 +10 초록 발판! (쿨다운 포함)                ║
-- ╠══════════════════════════════════════════════════════════════╣
-- ║  📦 준비물: Part (초록색, 넓은 발판) + Script               ║
-- ║  ⚠️ LeaderstatsScript 필수!                                ║
-- ║                                                              ║
-- ║  💡 이 발판은 Destroy 안 함! (계속 사용 가능!)              ║
-- ║  💡 대신 debounce로 2초 쿨다운 적용!                        ║
-- ║                                                              ║
-- ║  🔑 DebounceCoin과의 차이점:                                 ║
-- ║     • DebounceCoin: collected=true → 다시 못 먹음 + 삭제    ║
-- ║     • 이 발판: giving=true → 2초 대기 → giving=false 복구! ║
-- ║     • 즉, 2초마다 반복해서 코인 받을 수 있어요!             ║
-- ╚══════════════════════════════════════════════════════════════╝

local part = script.Parent

-- 💡 giving = "지금 코인을 주는 중인가?"
-- 💡 false = 줄 준비 됨! / true = 쿨다운 중!
local giving = false

part.Touched:Connect(function(hit)
	-- 💡 not giving = 쿨다운 중이 아닐 때만!
	if not giving then
		local player = game.Players:GetPlayerFromCharacter(hit.Parent)

		if player then
			giving = true   -- 💡 쿨다운 시작!

			local coins = player.leaderstats.Coins
			coins.Value = coins.Value + 10
			print("💰 " .. player.Name .. " → +10코인! (현재: " .. coins.Value .. ")")

			-- 💡 task.wait(2) = 2초 대기 (이 동안 Touched 무시!)
			task.wait(2)

			giving = false   -- 💡 쿨다운 끝! 다시 받을 수 있음!
		end
	end
end)
