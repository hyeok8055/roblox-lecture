-- ╔══════════════════════════════════════════════════════════════╗
-- ║  🔄 사라졌다 다시 나타나는 코인! (재생성)                    ║
-- ╠══════════════════════════════════════════════════════════════╣
-- ║  📦 준비물: Part (파란색, Cylinder, Neon) + Script           ║
-- ║  ⚠️ LeaderstatsScript 필수!                                ║
-- ║                                                              ║
-- ║  🔑 Destroy() vs 숨기기 비교:                                ║
-- ║     💥 Destroy() = 완전 삭제! 되돌릴 수 없음!              ║
-- ║     👻 숨기기 = 투명하게 해서 안 보이게! 나중에 복구 가능!  ║
-- ║                                                              ║
-- ║  💡 숨기기 방법:                                             ║
-- ║     1. Transparency = 1 → 완전 투명 (안 보임!)             ║
-- ║     2. CanCollide = false → 통과 가능 (못 밟음!)            ║
-- ║     3. task.wait(5) → 5초 대기                              ║
-- ║     4. Transparency = 0 → 다시 보임!                        ║
-- ║     5. collected = false → 다시 수집 가능!                  ║
-- ║                                                              ║
-- ║  💡 이 패턴은 체력 회복 아이템, 파워업 등에도 쓸 수 있어요! ║
-- ╚══════════════════════════════════════════════════════════════╝

local part = script.Parent
local collected = false

-- 💡 BillboardGui 찾기! (코인 위에 뜨는 라벨)
local coinGui = part:WaitForChild("CoinGui")

part.Touched:Connect(function(hit)
	if not collected then
		local player = game.Players:GetPlayerFromCharacter(hit.Parent)

		if player then
			collected = true
			player.leaderstats.Coins.Value += 1
			print("🔄 " .. player.Name .. " → 재생성 코인! +1")

			-- ════════════════════════════════════════
			-- 👻 코인 숨기기!
			-- ════════════════════════════════════════
			part.Transparency = 1         -- 💡 투명도 1 = 완전 투명!
			part.CanCollide = false        -- 💡 통과 가능! (Touched 안 됨)
			coinGui.Enabled = false       -- 💡 BillboardGui도 숨기기!

			-- 💡 5초 동안 안 보임!
			task.wait(5)

			-- ════════════════════════════════════════
			-- ✨ 코인 다시 나타나기!
			-- ════════════════════════════════════════
			part.Transparency = 0         -- 💡 투명도 0 = 완전히 보임!
			part.CanCollide = false        -- 💡 코인이니까 통과 유지!
			coinGui.Enabled = true        -- 💡 BillboardGui도 다시 보이기!
			collected = false              -- 💡 다시 수집할 수 있게!

			print("✨ 재생성 코인 복구!")
		end
	end
end)
