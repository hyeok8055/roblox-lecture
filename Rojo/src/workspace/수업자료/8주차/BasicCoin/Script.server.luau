-- ╔══════════════════════════════════════════════════════════════╗
-- ║  🪙 가장 기본적인 코인 줍기! (입문용)                        ║
-- ╠══════════════════════════════════════════════════════════════╣
-- ║  📦 준비물:                                                  ║
-- ║     • Part (Shape: Cylinder, 금색, Neon 소재)               ║
-- ║     • Anchored ✓ (공중에 고정!)                             ║
-- ║     • CanCollide ✗ (플레이어가 통과 가능!)                  ║
-- ║     • Part 안에 Script 넣기!                                ║
-- ║                                                              ║
-- ║  ⚠️ ServerScriptService에 LeaderstatsScript 필수!           ║
-- ║                                                              ║
-- ║  💡 동작 순서:                                               ║
-- ║     1️⃣ 플레이어가 코인을 밟음 (Touched)                    ║
-- ║     2️⃣ 밟은 캐릭터에서 플레이어 찾기                       ║
-- ║         (GetPlayerFromCharacter)                             ║
-- ║     3️⃣ 플레이어의 코인 +1                                  ║
-- ║     4️⃣ 코인 파트 삭제 (Destroy)                            ║
-- ║                                                              ║
-- ║  ⚠️ 주의: debounce가 없어서 간혹 중복 수집될 수 있어요!    ║
-- ║     → DebounceCoin에서 해결 방법을 배워요!                  ║
-- ╚══════════════════════════════════════════════════════════════╝

-- 💡 script.Parent = 이 스크립트가 들어있는 파트!
-- 💡 즉, 코인 파트 자체를 가리킴
local part = script.Parent

-- ════════════════════════════════════════
-- 👣 누군가 코인을 밟으면 실행!
-- ════════════════════════════════════════
-- 💡 Touched 이벤트 = 다른 파트가 닿으면 발생
-- 💡 hit = 닿은 그 파트 (보통 캐릭터의 발!)
-- 💡 hit.Parent = 캐릭터 모델 전체 (Head, Torso, 등...)
part.Touched:Connect(function(hit)

	-- 💡 GetPlayerFromCharacter(캐릭터모델)
	--    = 이 캐릭터의 주인(플레이어)를 찾아줌!
	-- 💡 NPC나 다른 파트가 닿으면 nil(없음) 반환
	local player = game.Players:GetPlayerFromCharacter(hit.Parent)

	-- 💡 player가 있으면 = 진짜 플레이어가 밟은 거!
	-- 💡 player가 nil이면 = NPC나 다른 파트가 닿은 것 (무시!)
	if player then
		-- 💡 플레이어 → leaderstats 폴더 → Coins IntValue
		-- 💡 .Value를 바꾸면 리더보드 숫자도 바로 바뀜!
		local coins = player.leaderstats.Coins
		coins.Value = coins.Value + 1   -- 💡 코인 +1!

		-- 💡 Output 창에 메시지 출력 (디버깅용)
		print("🪙 " .. player.Name .. " → 코인 획득! +1 (현재: " .. coins.Value .. ")")

		-- 💡 Destroy() = 파트를 게임에서 완전히 삭제!
		-- ⚠️ 한 번 Destroy하면 되돌릴 수 없어요!
		-- ⚠️ 다시 나타나게 하려면 → RespawnCoin 방식 사용!
		part:Destroy()
	end
end)
