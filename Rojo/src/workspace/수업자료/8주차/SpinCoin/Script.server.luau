-- ╔══════════════════════════════════════════════════════════════╗
-- ║  💫 빙글빙글 회전하는 코인!                                  ║
-- ╠══════════════════════════════════════════════════════════════╣
-- ║  📦 준비물: Part (금색, Cylinder, Neon, Anchored) + Script  ║
-- ║  ⚠️ LeaderstatsScript 필수!                                ║
-- ║                                                              ║
-- ║  🔑 5B 복습 개념들:                                          ║
-- ║     • while true do ... end = 무한 반복!                    ║
-- ║     • CFrame = 위치 + 회전 정보를 담은 값                   ║
-- ║     • CFrame.Angles(x, y, z) = 회전 각도 만들기            ║
-- ║     • math.rad(각도) = 도(degree)를 라디안으로 변환         ║
-- ║     • task.wait() = 한 프레임 대기 (약 1/60초)             ║
-- ║                                                              ║
-- ║  💡 동작 원리:                                               ║
-- ║     매 프레임마다 Y축으로 5도씩 회전!                       ║
-- ║     part.CFrame = part.CFrame * CFrame.Angles(0, 5도, 0)   ║
-- ║     현재 위치/회전 × 추가 회전 = 조금씩 돌아감!            ║
-- ║                                                              ║
-- ║  ⚠️ 주의! while true do는 아래 코드가 절대 실행 안 됨!     ║
-- ║     → 그래서 Touched는 while 위에 작성해야 해요!            ║
-- ╚══════════════════════════════════════════════════════════════╝

local part = script.Parent
local collected = false

-- ════════════════════════════════════════
-- 🪙 코인 수집 기능 (while 위에 작성!)
-- ════════════════════════════════════════
-- ⚠️ 이 코드가 while 아래에 있으면 절대 실행 안 돼요!
-- 💡 while true do가 무한 반복이라 그 아래로 절대 안 내려감!
part.Touched:Connect(function(hit)
	if not collected then
		local player = game.Players:GetPlayerFromCharacter(hit.Parent)

		if player then
			collected = true
			player.leaderstats.Coins.Value += 1
			print("💫 " .. player.Name .. " → 회전 코인! +1")
			part:Destroy()
		end
	end
end)

-- ════════════════════════════════════════
-- 🔄 회전 애니메이션! (무한 반복)
-- ════════════════════════════════════════
-- 💡 while true do = 영원히 반복! (멈추려면 파트를 Destroy!)
while true do
	-- 💡 현재 CFrame에 Y축 5도 회전을 곱하기!
	-- 💡 * = 곱하기 (CFrame끼리 곱하면 회전이 합쳐짐!)
	-- 💡 math.rad(5) = 5도를 라디안으로 변환
	--    (컴퓨터는 라디안을 사용하기 때문!)
	part.CFrame = part.CFrame * CFrame.Angles(0, math.rad(5), 0)

	-- 💡 task.wait() = 다음 프레임까지 대기 (약 0.016초)
	-- 💡 이게 없으면 한 프레임에 너무 많이 돌아서 게임이 멈춰요!
	task.wait()
end
-- ⚠️ 여기는 절대 도달하지 않음! (while true가 무한 반복이니까!)
