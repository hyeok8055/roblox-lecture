-- ╔══════════════════════════════════════════════════════════════╗
-- ║  🛡️ 중복 수집 방지 코인! (debounce 패턴)                   ║
-- ╠══════════════════════════════════════════════════════════════╣
-- ║  📦 준비물: Part (Cylinder, 금색, Neon) + Script            ║
-- ║  ⚠️ LeaderstatsScript 필수!                                ║
-- ║                                                              ║
-- ║  🤔 왜 debounce가 필요할까?                                  ║
-- ║     → Touched는 밟는 동안 매 프레임마다 여러 번 발생!       ║
-- ║     → 코인을 1번 밟았는데 10번 수집되는 버그 발생!          ║
-- ║     → collected 변수로 "이미 먹었는지?" 체크하면 해결!      ║
-- ║                                                              ║
-- ║  💡 debounce 동작 원리:                                      ║
-- ║     collected = false  (아직 안 먹음)                        ║
-- ║     → 밟음! → if not collected then (아직 안 먹었으니 OK!)  ║
-- ║     → collected = true (먹었다고 표시!)                      ║
-- ║     → Destroy() (파트 삭제!)                                ║
-- ║     → 다시 Touched 발생해도 collected=true라 무시됨!        ║
-- ╚══════════════════════════════════════════════════════════════╝

local part = script.Parent

-- ════════════════════════════════════════
-- 🛡️ debounce 변수 선언!
-- ════════════════════════════════════════
-- 💡 collected = "이미 수집했는가?" 를 기억하는 변수
-- 💡 false = 아직 안 먹음 → 수집 가능!
-- 💡 true = 이미 먹음 → 수집 불가!
local collected = false

part.Touched:Connect(function(hit)
	-- 💡 not collected = collected가 false일 때만 true
	-- 💡 즉, "아직 안 먹었을 때만" 이 안으로 들어옴!
	if not collected then
		local player = game.Players:GetPlayerFromCharacter(hit.Parent)

		if player then
			-- 💡 먼저 collected를 true로! (다른 Touched 막기!)
			collected = true

			-- 💡 코인 +1!
			player.leaderstats.Coins.Value += 1
			print("🛡️ " .. player.Name .. " → 코인! +1 (debounce로 1번만!)")

			-- 💡 파트 삭제!
			part:Destroy()
		end
	end
	-- 💡 collected가 true면 여기서 끝! (아무것도 안 함)
end)
