-- ╔══════════════════════════════════════════════════════════════╗
-- ║  🔊 효과음이 나는 코인!                                      ║
-- ╠══════════════════════════════════════════════════════════════╣
-- ║  📦 준비물: Part (주황색, Cylinder, Neon) + Script           ║
-- ║  ⚠️ LeaderstatsScript 필수!                                ║
-- ║                                                              ║
-- ║  🔑 새로운 개념: Sound (소리 객체)                           ║
-- ║     • Instance.new("Sound") = 소리 객체 만들기              ║
-- ║     • SoundId = "rbxassetid://숫자" (에셋 ID)              ║
-- ║     • Volume = 음량 (0 = 무음, 1 = 보통, 10 = 최대)       ║
-- ║     • sound:Play() = 소리 재생!                             ║
-- ║     • sound.Parent = part (파트 안에 넣어야 위치에서 들림!) ║
-- ║                                                              ║
-- ║  🎵 추천 효과음 ID:                                          ║
-- ║     • 코인 줍기: 9125402735                                 ║
-- ║     • 레벨업:    9125468737                                  ║
-- ║     • 문 열림:   1937484899                                  ║
-- ║     • 폭발:      9114027021                                  ║
-- ║                                                              ║
-- ║  💡 10주차에서 사운드를 더 자세히 배울 거예요!              ║
-- ╚══════════════════════════════════════════════════════════════╝

local part = script.Parent
local collected = false

-- ════════════════════════════════════════
-- 🔊 효과음 미리 만들기! (스크립트 시작할 때 1번만!)
-- ════════════════════════════════════════
-- 💡 Instance.new("Sound") = 새로운 소리 객체 생성
local sound = Instance.new("Sound")

-- 💡 SoundId = 재생할 소리의 에셋 ID
-- 💡 "rbxassetid://숫자" 형식! (Roblox 라이브러리에서 찾기)
sound.SoundId = "rbxassetid://9125402735"

-- 💡 Volume = 음량 설정
-- 💡 0 = 무음, 0.5 = 절반, 1 = 보통, 2 = 2배
sound.Volume = 1

-- 💡 Parent = 이 소리가 어디서 들릴지!
-- 💡 파트 안에 넣으면 → 그 파트 근처에서 들림! (3D 사운드)
sound.Parent = part

-- 💡 누군가 밟으면!
part.Touched:Connect(function(hit)
	if not collected then
		local player = game.Players:GetPlayerFromCharacter(hit.Parent)

		if player then
			collected = true
			player.leaderstats.Coins.Value += 1
			print("🔊 " .. player.Name .. " → 효과음 코인! +1 ♪")

			-- 💡 sound:Play() = 소리 재생 시작!
			-- 💡 소리는 백그라운드에서 재생됨 (코드가 멈추지 않음!)
			sound:Play()

			-- 💡 효과음이 들릴 시간을 준 후 삭제!
			task.wait(0.5)
			part:Destroy()
		end
	end
end)
